import { admin } from './admin';
import { auth } from './auth';
import { event } from './event';
import { info } from './info';

const queryKey = { admin, auth, event, info };

export default queryKey;
