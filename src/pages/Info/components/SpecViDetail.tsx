import { runningSpecViGetResponse } from '@/apis/types/info';

interface SpecViDetailProps {
  data: runningSpecViGetResponse;
}

const SpecViDetail: React.FC<SpecViDetailProps> = () => {
  return <>SpecDetail</>;
};

export default SpecViDetail;
